apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: error-service
  namespace: argocd
spec:
  generators:
  - list:
      elements:
      - env: dev
        namespace: dev-backend
  template:
    metadata: 
      name: '{{env}}-error-service'
      labels: 
        env: '{{env}}'
        scope: backend
        type: app
        name: 'error-service'
    spec:
      project: '{{env}}'
      destination:
        namespace: '{{namespace}}'
        name: 'in-cluster'
      source:
        repoURL: 'https://github.com/dbgjerez/service-mesh-workshop'
        path: apps/error-service/gitops
        targetRevision: develop
        helm:
          valueFiles:
            - '{{env}}.values.yaml'
      syncPolicy:
        automated: {}